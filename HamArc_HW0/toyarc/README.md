# toyarc (stub)

Заглушка для мини-программы `toyarc`.

Предлагаемая структура:

- `include/args.hpp` — структуры/enum-ы для результатов парсинга.
- `src/main.cpp` — точка входа и логика разбора аргументов.
- `tests/` — при желании можно добавить простые проверки.
