# copyfile (stub)

Заглушка для мини-программы `copyfile`.

Предлагаемая структура:

- `include/copy.hpp` — интерфейс функции поблочного копирования.
- `src/main.cpp` — точка входа и работа с аргументами/сообщениями об ошибках.
- `tests/` — при желании можно добавить небольшие проверки.

